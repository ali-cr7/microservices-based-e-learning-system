# Service name for Eureka registration
spring.application.name=user-service

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/elearning_users
spring.datasource.username=springstudent
spring.datasource.password=springstudent
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# Server port (optional, but recommended to avoid conflicts)
server.port=8081

# Logging
logging.level.root=warn

# Turn off the Spring Boot banner
spring.main.banner-mode=off

jwt.secret=m-QygAmEI-xOl8UW1ZokkOjgNGXdkZrmrAu7GRCpbUs=